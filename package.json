{
  "name": "wikipediaviewerreact",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "query": "*",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4"
  },
  "proxy": "http://localhost:3004", 
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "yarn run build && surge -p build -d dark-yarn.surge.sh"
  }
}
